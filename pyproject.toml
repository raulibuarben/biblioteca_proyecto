[tool.poetry]
name = "biblioteca"
version = "0.1.0"
description = "Gestor para una biblioteca"
authors = ["raulibuarben <raulibu@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
